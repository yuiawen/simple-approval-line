<?xml version="1.0" encoding="utf-8"?>
<odoo>
    
    <record id="view_approval_line_form" model="ir.ui.view">
        <field name="name">approval.line.form</field>
        <field name="model">approval.line</field>
        <field name="arch" type="xml">
            <form string="Approval Line" create="false" edit="false">
                <header>
                    <button name="action_approve" 
                            string="Approve" 
                            type="object" 
                            class="oe_highlight"
                            invisible="state != 'pending' or approver_id != uid"/>
                    <button name="action_reject" 
                            string="Reject" 
                            type="object" 
                            class="btn-danger"
                            invisible="state != 'pending' or approver_id != uid"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="res_name" readonly="1"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="sequence" readonly="1"/>
                            <field name="approver_id" readonly="1"/>
                        </group>
                        <group>
                            <field name="approval_date" readonly="1"/>
                        </group>
                    </group>
                    <group string="Notes">
                        <field name="notes" nolabel="1" placeholder="Add approval notes here..."/>
                    </group>
                    <group invisible="1">
                        <field name="res_model"/>
                        <field name="res_id"/>
                        <field name="approver_name"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>